<p align="center">
  <a href="README.md">🇺🇸 English</a> |
  <a href="README_FA.md">🇮🇷 فارسی</a>
</p>

# نسخه فارسی (PasBan) 🛡️

پاس‌بان یک اپلیکیشن امن، مدرن و متن‌باز برای احراز هویت دو مرحله‌ای (2FA) است که با فریم‌ورک فلاتر توسعه داده شده است. این اپلیکیشن کدهای یک‌بارمصرف زمان‌دار (TOTP) تولید می‌کند و تمامی اطلاعات شما را با بالاترین سطح امنیت و با استفاده از رمزنگاری‌های پیشرفته، فقط در داخل گوشی خودتان ذخیره می‌کند.

## ✨ امکانات

* **اسکنر بارکد (QR Code):** افزودن سریع اکانت‌ها تنها با اسکن کدهای استاندارد احراز هویت.
* **ورود دستی:** امکان اضافه کردن اکانت از طریق وارد کردن کلید امنیتی (Setup Key) به همراه سیستم هوشمند اصلاح خطاهای تایپی.
* **قفل بیومتریک و احراز هویت بومی:** محافظت از ورود به اپلیکیشن با استفاده از سنسور اثر انگشت یا تشخیص چهره (Face ID / Fingerprint) جهت جلوگیری از دسترسی‌های غیرمجاز.
* **تولید زنده و ۳۰ ثانیه‌ای رمزها:** تولید کدهای ۶ رقمی در لحظه که هر ۳۰ ثانیه به صورت خودکار تغییر می‌کنند، همراه با تایمر گرافیکی دایره‌ای (Pie-chart) و انیمیشن شمارش معکوس.
* **پشتیبانی از چندزبانی:** بومی‌سازی کامل و امکان تغییر پویای زبان برنامه بین انگلیسی و فارسی (با چیدمان کاملاً راست‌چین RTL).
* **مدیریت پیشرفته تم:** هماهنگی کامل و امکان جابجایی بین حالت تاریک (Dark Mode)، روشن (Light Mode) و تم پیش‌فرض سیستم‌عامل.
* **ذخیره‌سازی امن:** تمام کلیدهای مخفی به صورت رمزنگاری‌شده در حافظه سخت‌افزاری و امن گوشی (Keystore در اندروید و Keychain در iOS) ذخیره می‌شوند.
* **مدیریت آسان اکانت‌ها:** امکان حذف کارت‌های منقضی شده یا تکراری با کشیدن کارت به سمت چپ (Swipe to Delete) همراه با دیالوگ تاییدیه حذف.
* **کپی با یک لمس:** کپی کردن سریع رمزها در کلیپ‌بورد با تجربه کاربری (UI/UX) روان و بی‌نقص.
* **کاملاً آفلاین:** این اپلیکیشن هیچ‌گونه دسترسی به اینترنت نمی‌خواهد و تضمین می‌کند کلیدهای شما هرگز از گوشی خارج نمی‌شوند.

## 🛠️ تکنولوژی‌ها و معماری

* **فریم‌ورک:** فلاتر (Flutter / Dart)
* **معماری:** معماری پاک (Clean Architecture) شامل لایه‌های Data، Domain و Presentation
* **مدیریت وضعیت (State Management):** الگوی BLoC (Business Logic Component)
* **مسیریابی:** پکیج GoRouter

### پکیج‌های کلیدی
* `flutter_secure_storage`: برای رمزنگاری و ذخیره امن اطلاعات در سطح سخت‌افزار دستگاه.
* `local_auth`: برای یکپارچه‌سازی سیستم احراز هویت بیومتریک سیستم‌عامل.
* `mobile_scanner`: برای اسکن سریع، هوشمند و دقیق دوربین.
* `otp`: برای پیاده‌سازی الگوریتم استاندارد جهانی کدهای یک‌بارمصرف زمان‌دار (RFC 6238).
* `flutter_bloc`: برای مدیریت تمیز و پیش‌بینی‌پذیر وضعیت‌های مختلف برنامه.

## 🚀 راه‌اندازی پروژه

برای کلون کردن و اجرای این پروژه روی سیستم خود، نیاز دارید که Git و Flutter روی کامپیوتر شما نصب باشند.

```bash
# کلون کردن مخزن
$ git clone [https://github.com/Lilfibonacci/PasBan.git](https://github.com/Lilfibonacci/PasBan.git)

# ورود به پوشه پروژه
$ cd PasBan

# دریافت پکیج‌ها
$ flutter pub get

# اجرای اپلیکیشن
$ flutter run
```
## 📥 دانلود
شما می‌توانید جدیدترین نسخه اپلیکیشن (APK) را مستقیماً از بخش Releases در همین مخزن دانلود کنید.

## 🤝 مشارکت
مشارکت شما باعث افتخار ماست! اگر ایده‌ای برای بهبود پروژه دارید:

۱. پروژه را Fork کنید.
۲. برنچ مخصوص قابلیت خود را بسازید (git checkout -b feature/NewFeature)
۳. تغییرات خود را Commit کنید (git commit -m "Add NewFeature")
۴. برنچ را Push کنید (git push origin feature/NewFeature)
۵. یک Pull Request ثبت کنید.

## 📄 لایسنس
این پروژه تحت لایسنس MIT منتشر شده است و استفاده از کدهای آن کاملاً آزاد است. برای اطلاعات بیشتر فایل LICENSE را مطالعه کنید.

## ❤️ سخن پایانی
هدف پاس‌بان ارائه یک راهکار امن، رایگان و قابل‌اعتماد برای احراز هویت است تا هویت دیجیتال کاربران حتی در شرایط سخت شبکه نیز کاملاً محفوظ بماند.
